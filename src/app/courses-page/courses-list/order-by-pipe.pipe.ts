// import { Pipe, PipeTransform } from '@angular/core';
// import { CoursesItem } from '../courses-item.model';

// @Pipe({
//   name: 'orderByPipe'
// })
// export class OrderByPipePipe implements PipeTransform {

//   transform(value: Array<CoursesItem>): Array<CoursesItem> {
//     value.sort((course1: CoursesItem, course2: CoursesItem) => {
//       if (course1.creationDate > course2.creationDate) return 1;
//       if (course1.creationDate < course2.creationDate) return -1;
//       if (course1.creationDate === course2.creationDate) return 0;
//     })
//     return value;
//   }

// }
